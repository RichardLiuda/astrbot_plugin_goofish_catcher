{
  "provider_mode": {
    "type": "string",
    "description": "抓取模式",
    "hint": "`playwright_local` 为本地浏览器，`remote_rest` 为远程 Worker",
    "options": [
      "playwright_local",
      "remote_rest"
    ],
    "default": "playwright_local"
  },
  "remote_base_url": {
    "type": "string",
    "description": "远程地址",
    "hint": "远程模式必填，例如 `https://worker.example.com`，不要带结尾斜杠",
    "default": ""
  },
  "remote_headers": {
    "type": "object",
    "description": "远程 Header",
    "hint": "Cloudflare Access 认证填写下方两项即可；如需其他 Header 请使用 `remote_headers_json`",
    "items": {
      "CF-Access-Client-Id": {
        "type": "string",
        "description": "CF-Access-Client-Id",
        "hint": "Cloudflare Access Service Token 的 Client ID",
        "default": ""
      },
      "CF-Access-Client-Secret": {
        "type": "string",
        "description": "CF-Access-Client-Secret",
        "hint": "Cloudflare Access Service Token 的 Client Secret",
        "default": ""
      }
    }
  },
  "remote_api_key": {
    "type": "string",
    "description": "远程 API Key",
    "hint": "远程 Worker 自己启用 API Key 鉴权时填写；走 Cloudflare Access 则留空",
    "default": ""
  },
  "llm_enabled": {
    "type": "bool",
    "description": "启用 LLM 推荐分析",
    "hint": "关闭后只用启发式规则，不调用大模型进行推荐排序",
    "default": true
  },
  "llm_provider_id": {
    "_special": "select_provider",
    "type": "string",
    "description": "推荐分析模型",
    "hint": "留空时优先使用当前会话模型，再回退到第一个可用模型",
    "default": ""
  },
  "llm_agent_enabled": {
    "type": "bool",
    "description": "启用浏览器 Agent",
    "hint": "开启后 LLM 可调用 goofish_browser_task 执行复杂页面操作（收藏、查卖家等）；关闭后只保留脚本化工具（搜索、订阅等不受影响）",
    "default": true
  },
  "llm_agent_provider_id": {
    "_special": "select_provider",
    "type": "string",
    "description": "浏览器 Agent 模型",
    "hint": "用于 goofish_browser_task 的 ReAct 推理；留空时回退到推荐分析模型",
    "default": ""
  },
  "llm_agent_headless": {
    "type": "bool",
    "description": "Agent 浏览器无头模式",
    "hint": "true = 后台静默运行；false = 显示浏览器窗口（便于调试）",
    "default": false
  },
  "admin_webui_enabled": {
    "type": "bool",
    "description": "启用管理后台",
    "hint": "开启后在插件进程内启动 WebUI，可在浏览器中配置超时、价格阈值等高级设置",
    "default": false
  },
  "admin_webui_port": {
    "type": "int",
    "description": "后台端口",
    "hint": "默认 `8790`，避免与其他服务冲突",
    "default": 8790
  },
  "admin_webui_api_key": {
    "type": "string",
    "description": "后台 API Key",
    "hint": "启用后台时必须配置，用于登录认证",
    "default": ""
  }
}
